# 踩坑记录与量化数据

> 这份文档记录的是开发过程中**真实踩到并修掉**的问题。
> 每一条都写了「现象 → 怎么发现的 → 根因 → 怎么修」，面试被追问时可以照着讲。
>
> 最后更新：2026-09-12

---

## 一、量化数据（可直接写进简历）

### 意图识别准确率

评估集 **63 条**人工标注样本（含口语化、隐含意图、跨域，并按"发布 vs 查询"
"跨域 vs 不该跨域"组织成对照组），跑 `mvn test -Dtest=IntentEvalTest -Deval.enabled=true` 可复现：

| 指标 | 数值 |
|---|---|
| 准确率 | **95.2%**（60/63） |
| 关键词层直出 | 40%（零成本、毫秒级） |
| 本地小模型判定 | 59%（本地、免费） |
| 大模型兜底 | **2%**（唯一有云端成本的部分） |

**关键结论：98% 的请求不产生模型费用。**

> ⚠️ **这个数字和之前那份对不上，是正常的，原因是评估集换了**——
> 原来是 26 条，报 96.2%；现在是 63 条，报 **95.2%**（修完第 51、52 条之后）。
> **不是系统变差了，是原来的尺子量不出来**：旧集合里"发布"只有 1 条，
> 而且恰好是"帮我发布一台出租"（词表专门为它写过），给错误行为背了书。
> 换成 63 条后基线一度掉到 87.3%（详见第 51、52 条），修完回到 95.2%。
>
> 简历上要用这个数字的话，**用 95.2%（60/63）**——样本更多、覆盖更全，
> 而且经得起追问"这 63 条是怎么标的"。

### 工程数据

| 项 | 数值 |
|---|---|
| 自动化测试 | 160 个，全绿 |
| 后端代码 | 100+ 个类 |
| 数据库表 | jxj 业务表只读 + 17 张 ai_ 表 |
| 知识库规模 | 12 条平台规则语料 + 4 篇资讯 |

### 检索质量（Recall@5，24 条标注 + 10 条负样本）

| 召回方式 | Recall@5 |
|---|---|
| 只用关键词（BM25） | 75.0% |
| 只用向量（KNN） | 95.8% |
| 混合（RRF） | 95.8% |

**证据闸**：向量判据的零漏放分界要 0.830（误拦 54%），换成精排后只要 0.191（误拦 13%）。
原阈值 0.6 实测漏放率 **100%**（形同虚设），修复后 **0%**。

### 一次真实的优化闭环

| 阶段 | 准确率 | 说明 |
|---|---|---|
| 初版（26 条评估集） | 92.3% | 评估发现"帮我发布一台出租"被"出租"抢走 |
| 修复后（26 条评估集） | 96.2% | 引入"动作意图优先"规则 |
| **扩到 63 条后的基线** | **87.3%** | **同一套代码**——不是变差了，是原来的尺子量不出来 |
| 再修复后（63 条评估集） | **95.2%** | 发布意图不再由关键词定案（第 51 条）+ 跨域不再被抢答（第 52 条） |

> ⚠️ **中间那两行是同一条线的完整故事**：26 条时报 96.2%，换成 63 条后
> **同一套代码**只有 87.3%——差的那 9 个百分点全在旧评估集没覆盖的地方
> （比如"发布"这一类它只有 1 条样本）。
> **简历上要用就用 95.2%（60/63）**，而且这个故事本身比数字更值钱。

---

## 二、依赖与版本类

### 1. Spring Boot 4.1 + MyBatis-Plus 3.5.13 启动即崩

**现象**：`mvn compile` 通过，一启动就 `NoSuchMethodError: PropertyMapper.alwaysApplyingWhenNonNull()`。

**根因**：MyBatis-Plus 3.5.13 调用的这个方法在 Spring Boot 4.1 已被移除（换成了 `alwaysApplying`）。

**教训（面试点）**：**「编译通过」不等于「能启动」**。M1 阶段我只跑了 compile 就认为骨架没问题，实际一引入 Mapper 就炸。后来把「必须真实启动 + 打健康接口」写成了每个里程碑的验收项。

**修法**：升级 MyBatis-Plus 到 3.5.17。

### 2. Jackson 2 与 Jackson 3 并存，误用会静默失败

**现象**：工具返回 `{"error":"结果序列化失败"}`，但日志里看不到异常。

**根因**：Spring Boot 4 和 Spring AI 2 都用 **Jackson 3**（`tools.jackson.*`），而项目里还留着 Jackson 2 的依赖。我写了 `new ObjectMapper()`（Jackson 2），它**没有注册 java.time 支持**，序列化带 `LocalDateTime` 的对象直接抛异常——而异常被工具层的兜底 try/catch 吞掉了。

**为什么难查**：`JsonInclude` 这类注解**仍在** `com.fasterxml.jackson.annotation` 包下（Jackson 3 复用了 2.x 的注解构件），所以"看到 com.fasterxml 的包名"会误导你以为在用 Jackson 2。

**教训（面试点）**：兜底 catch 要打得出来日志。当时如果只是 `catch (Exception e) {}`，这个问题会一直查不到。

**修法**：移除 Jackson 2 依赖，统一用 `tools.jackson.*`。

### 3. Spring AI 2.0 的 OpenAI 客户端换成了官方 SDK

Ollama 用小模型时，原本想用「OpenAI 兼容端点」接入。但 Spring AI 2.0 的 OpenAI 客户端底层已换成官方 `openai-java` SDK，要指向 Ollama 得自己搭 `OpenAIClient`。改用官方的 Ollama starter 更省事。

**顺带一个坑**：Ollama starter 默认也会注册一个 `EmbeddingModel`，和 DashScope 那个冲突。必须显式锁定 `spring.ai.model.embedding: openai`。

---

## 三、数据与领域类（jxj 真实库）

### 4. 逻辑删除配置会把 14 条在租记录全部过滤掉

**现象**：配了 MyBatis-Plus 的全局逻辑删除后，出租列表一条都查不出来。

**根因**：`jxb_chuzu`/`jxb_qiuzu` 的**活动行 `del_flag` 是空串或 NULL**，只有被删除的行才是 `'1'`。而全局逻辑删除会补 `del_flag = '0'`，一条都匹配不上。

**教训（面试点）**：**别假设历史库的字段语义跟框架默认一致**。RuoYi 这套老系统里，各表的 `del_flag` 含义并不统一。

**修法**：去掉全局逻辑删除配置，改由 `BizFilters` 显式写明每张表的可见性条件（判据是"不等于 `'1'`"而非"等于 `'0'`"）。

### 5. 72 台设备里只有 23 台对用户可见

**现象**：设备查询把库里 72 行全查出来了，其中大量是 `status`/`price` 全空的脏数据。

**根因**：平台的可见性口径是 `shelves_status = 1`（上架），另外 49 行是 `type=0` 的目录行。这个口径写在 RuoYi 的 mapper XML 里，**看表结构推不出来**。

**修法**：所有可见性条件收口到 `BizFilters`，照抄原系统的判据。

### 6. 品牌字段是个陷阱

`jxb_equipment.brand_id` 指向 `pms_brand`，但**库里的数据跟设备名对不上**（名字是"日立ZX200-3挖掘机"的车，`brand_id` 却指向三一重工）。真正可信的是 `second_cate_id`（指向 `sys_category` 的二级分类）。

**教训**：**同一个业务概念有多个字段时，要确认哪个是真的在用**。判断依据是看原系统的查询代码，不是看列注释。

---

## 四、框架用法类（LangGraph4j）

### 7. 并行扇出没有公开 API

**现象**：想搭"多个 Agent 并行跑完再汇总"的图，翻遍 `StateGraph` 只找到 `addEdge(String, String)`，没有任何多源汇合的重载。

**怎么查的**：反编译 jar 读字节码，确认——没有 `addEdge(List, String)`、没有 join/barrier、`Command.gotoNode` 是单个 String。

**真相**：并行是**隐式**的。给同一个源节点挂多条出边，编译期自动生成一个合成并行节点 `__PARALLEL__(源id)`；**所有分支必须汇合到同一个后继**，否则 `compile()` 直接抛错。

**另一个坑**：`ParallelNode` 是 public 的，但它由编译器生成，**不该自己 new**（我最初的方案就是自己构造，是错的）。

**修法**：
```java
g.addEdge(START, "A");  g.addEdge("A", "synthesize");
g.addEdge(START, "B");  g.addEdge("B", "synthesize");  // 同一个后继
```

### 8. 并行分支里抛异常会让整轮失败

**根因**：LangGraph4j 的并行节点内部用 `allOfFailFast` 等所有分支。

**教训（面试点）**：**并行分支必须自己吞异常**。一个域查不到不该拖垮其它域。这是"并行执行"容易被忽略的一环——串行时一个失败只影响后续，并行时会全灭。

### 9. 并行写同一个 key 会互相覆盖

**现象**：跨域综合时，三个域的结果只剩最后一个。

**根因**：LangGraph4j 的普通 channel 是"后写覆盖先写"。

**修法**：改用 `Channels.appender(ArrayList::new)`，把每次写入追加进同一个 List。

### 10. 构图必须显式给状态序列化器

`ReactAgentBuilder` 不给 `stateSerializer` 时，构图直接抛 `NullPointerException: stateSerializer is required!`。而且要用 Spring AI 专用的 `SpringAIJacksonStateSerializer`——通用的 Jackson 序列化器处理不了消息对象里的多态字段。

---

## 五、模型行为类（最值得讲的部分）

### 11. 本地小模型冷启动导致分类静默降级 ⭐

**现象**：同一个问题"小松200-8空调故障怎么解决"，一次判对、一次判成"未知"。

**怎么发现的**：审计日志里只记了路由结论，看不到分类依据。**先补上"记下分类器的原始判定"，再重跑**，才发现日志里两次都是 `TimeoutException`。

**根因**：模型调用统一设了 8 秒超时。但——
- Ollama 首次调用要把几 GB 权重读进内存（冷启动十几秒很正常）
- 主模型 MIMO 是**推理型**，会先"想"一段再输出

两层双双超时 → 这句话被判成 UNKNOWN → **静默降级到兜底 Agent**。用户只觉得"答得敷衍"，从现象上根本看不出是超时。

**教训（面试点，很适合主动讲）**：
1. **静默降级比报错更危险**——功能看起来"能用"，只是质量差，没人会发现
2. **超时要按层设置**：本地模型冷启动、推理型模型的思考时间，都不是一个固定值能覆盖的
3. **可观测性要先做**——不先把"判定依据"记进审计，这个 bug 根本查不出来

**修法**：按层设超时（小模型 30s / 主模型 45s / 生成 60s），并加启动后异步预热本地模型。

### 12. 推理型模型会把 token 全花在"思考"上

`mimo-v2.5` 是推理模型，响应里有 `reasoning_content`。实测一句 "Reply with exactly: OK" 配 `max_tokens: 20`，19 个 token 全用在推理上，**`content` 返回空字符串且不报错**。

**教训**：给推理模型设 `max_tokens` 要留足余量；解析响应时不能假设"有 choices 就一定有内容"。

### 13. 大模型会编平台规则（所以必须有证据闸）

问"在平台租设备的流程和押金怎么算"，知识库里没有相关内容时，大模型的反应**不是**说"我不知道"，而是凭对同类电商平台的印象编出押金比例、手续费率、退款时限——**编得像模像样，用户没法分辨**。

**修法**：证据闸在生成**之前**判断资料够不够；不够就不让它开口。注意闸必须在生成前——生成完再撤回，错话已经发出去了。

补充一层保险：生成后再让本地小模型核对"答案是否完全基于资料"。

### 14. 小模型对错误答案也给高分

qwen2.5:7b 直连测试时，把"小松200-8空调故障怎么解决"判成 `EQUIPMENT_QUERY`（设备买卖），置信度 0.8。

**教训**：**模型自报的置信度不能全信**。所以每层除了置信度阈值，还额外要求"意图不能是 UNKNOWN"，而且小模型的门槛（0.6）设得比大模型（0.4）高——小模型更容易自信地答错。

---

## 六、设计与算法类

### 15. 跨域问题被单 Agent 静默吃掉 ⭐

**现象**：用户问"有没有20吨挖机、附近能租吗、流程是啥"，只答了其中一部分。

**根因**：跨域意图被丢进了单 Agent 匹配，而规则是"能力交集最多者胜"。`CROSS_DOMAIN` 声明了 5 个能力，RentalAgent 交集 3 个、EquipmentAgent 只 1 个 → **整条交给租赁 Agent**，而它手里根本没有设备、资讯工具。

**特别值得讲的一点**：`ai_agent.priority`（设备 100 > 租赁 90）在这条路径上**根本没参与比较**——因为先比交集个数，平手才看优先级。**配置项写了不等于会生效**。

**修法**：跨域走独立的综合子图，不经过单 Agent 匹配。

### 16. 两个高权重词必然同分 → 歧义判不出来

所有高权重关键词都是 0.9 分。所以"二手设备出租"会同时命中设备(0.9)和出租(0.9)，**必然同分**，而单意图定案要求"严格高于第二名"，于是永远判不出来。

**这个特性有两个后果**：
- 它是跨域判定必须排在单意图定案**之前**的原因——单域那条路对"一次命中多个域"的输入天然无解
- 反过来，它也是个保护：不确定就不硬猜，下沉给模型

### 17. RRF 融合而不是加权求和

BM25 的分数**无上界**（几分到几十分），向量相似度是 0~1。直接加权求和的话，BM25 会完全盖住向量那一路。

RRF 只用"名次"不用"分数"：某条结果在任一路排第 r 名就得 `1/(k+r)` 分。不需要归一化，也不怕两路量纲不同。

### 18. 证据闸里有一段永远走不到的分支

**现象**：写测试时发现某个分支怎么都触发不了。

**根因**：原实现先判"达标条数够不够"、再判"最高分够不够"。但达标条数 ≥1 就意味着最高分必然达标——**第二个判断在第一关放行后永远不可能触发**。

**教训（面试点）**：**死分支不是无害的**，它会让读代码的人以为"这个情况被覆盖了"。写测试时发现"这个 case 构造不出来"，往往就是代码有问题，而不是测试写得不对。

**修法**：两个判据各管一件事——先排除"压根没有相关资料"，再排除"只有一条勉强沾边"（后者要 `min-evidence-count > 1` 才有实际约束）。

### 19. 发布意图被领域词抢走（评估发现的）

**现象**：评估集里"帮我发布一台出租"被判成"出租查询"。

**根因**：发布意图的高权重词写的是整串"帮我发布出租"，用户中间插个"一台"就匹配不上；而"出租"是出租查询的高权重词，命中 0.9 且无对手 → 关键词层直出。

**修法**：引入**动作意图优先**——发布是"动作"不是"查询"，命中高权重词就直接定案，且不参与跨域判定（否则"发布+出租"会被当成两个域）。

**效果**：准确率 92.3% → **96.2%**（当时的 26 条评估集）。
> ⚠️ 这个数字后来被更大的评估集推翻了——详见第 51 条：尺子换了之后基线是 87.3%。

### 20. 关键词设计要避开"领域名词"

最初的设备意图高权重词里放了"挖掘机""装载机"。问题是这些机型名词在**出租、求租、需求**里都会出现，结果"附近有挖掘机出租吗"会被误判成设备查询。

**修正原则**：高权重词只放**几乎唯一指向该意图**的词（"求租""转人工"）；领域名词一律不放。

---

## 七、工程实践类

### 21. 测试账号删不干净

**现象**：跑了几轮测试后，`ai_user` 表里积了 130 行垃圾数据。

**根因**：测试清理用了 MyBatis-Plus 的 `deleteById`，而它走的是 `@TableLogic` **逻辑删除**——只把 `del_flag` 置成 `'2'`，行还留在表里。

**修法**：改用 `JdbcTemplate` 物理删除。

### 22. MockMvc 在 Spring Boot 4.1 里没有自动配置

`@AutoConfigureMockMvc` 在 Boot 4.1 的依赖里根本不存在（该自动配置未随 4.x 发布）。改为从 `WebApplicationContext` 手工构建：

```java
MockMvcBuilders.webAppContextSetup(webApplicationContext).build();
```

效果等价——DispatcherServlet 的拦截器照样执行，鉴权能被真实验证到。

### 23. 循环依赖：配置类 → 拦截器 → 服务 → 配置类

`WebConfig`（持有拦截器）→ `AuthInterceptor` → `AuthService` → 需要 `BCryptPasswordEncoder` 这个 Bean，而它定义在 `WebConfig` 里，形成环。

**修法**：把密码编码器拆到独立的 `PasswordConfig`；同时让拦截器只依赖 `TokenStore`（它本来就只需要解析令牌，不需要整个 AuthService）。

### 24. 启动时的日志刷屏

MyBatis-Plus 的 `StdOutImpl` 会把每条 SQL 的**完整结果集**打出来——启动时载入 3869 条区划 + 438 条字典就是几千行。

**修法**：去掉 `log-impl`，需要时用 `logging.level.<mapper包>: debug` 临时打开。

### 25. 自评误杀正确答案 ⭐（提示词反向暗示）

**现象**：问"平台收多少手续费"，知识库里明明有"平台收取多少服务费"这条（相关度 0.80，证据闸放行了），生成的回答也**完全正确**（3% / 5% / 5000 元上限，每个数字都能在语料里找到），但被自评判为不合格、改成"请转人工"。

**根因**：自评提示词里写的是——

> 如果回答里出现了资料中没有的具体数字、比例、时限、条款，判为"否"。

本意是让它核对数字，**实际效果是暗示它"看到数字就判否"**。7B 的小模型对这类暗示很敏感，看到回答里一串百分比就直接给了"否"，没去资料里核对。

**为什么难发现**：这个 bug 的表现是"用户明明能拿到答案，却被拒答"，从用户侧看就是"这机器人什么都不会"。而日志里只留一句"自评未通过"，**不记模型到底说了什么**，根本查不出是误杀还是真拦。

**修法**：
1. 提示词改成"逐条核对 + 给正例"——先去找依据，而不是先怀疑；
2. **把模型的原始判定打进日志**，误杀才能被发现。

**教训（面试点）**：提示词里的否定句要小心——**"如果出现 X 就判否"会把模型的注意力引到 X 上**，而不是让它去验证。防幻觉的提示词尤其容易写成这样。

### 26. 测试数据泄漏：同一个坑踩了两次

**现象**：知识库里多出 8 条 `test-*` 的测试数据没清掉。

**根因**：测试清理用了 `docMapper.deleteById`，而它走 `@TableLogic` **逻辑删除**——只把 `del_flag` 置成 `'2'`，行还在表里；而清理时的查询会自动过滤掉这些行，**下一轮就再也看不见它们了**。

**这个坑在 `ai_user` 上已经踩过一次并修了**（见第 21 条，属工程实践类），知识库这边忘了同步。教训是：**修完一个坑要顺带搜一遍同类代码**，而不是只修眼前那一处。

**修法**：改用 `JdbcTemplate` 物理删除，并额外清理孤儿切片。

**同一个地方又栽了第三次（顺序写反）**：修完物理删除之后，ES 里**还是**在积测试数据——
跑几轮就多出十几条 `[测试]` 文档，而且真的会被检索到、挤掉正确答案。

**根因**：`@AfterEach` 里写成了

```java
jdbcTemplate.update("DELETE FROM ai_knowledge_doc WHERE ...");   // ① 先删了 MySQL
List<AiKnowledgeDoc> docs = docMapper.selectList(...);           // ② 再查——已经查不到了
for (AiKnowledgeDoc doc : docs) { es.deleteByQuery(...); }       // ③ 循环体一次都没执行
```

先删了数据源，再回头去数据源里查 id 来删索引——**②永远返回空，③是个死循环**。
而且它**不报错、不告警**，只是悄悄什么都不做。

**修法**：顺序倒过来（先查、后删），删 ES 时显式 `refresh`，
最后再跑一次 `reconcileIndex()` 对账兜底——对账走的是线上同一套逻辑，
比再补一个 `sleep` 靠谱。

**教训（很值得讲）**：这是一个**"清理代码自己坏了但没人发现"**的例子。
清理逻辑和业务逻辑不一样，它坏了不会有任何现象，只会让脏数据慢慢积累，
等到"检索结果莫名其妙变差"的时候，已经查不出是哪来的了。
所以清理逻辑也要有**可验证的收敛判据**——这里是"跑完测试套件后索引文档数应当不变"，
比"看起来清干净了"可靠得多。

### 27. ES 索引被自动创建，映射是错的

**现象**：删掉索引后没重启应用，直接触发入库——ES **自动创建了同名索引**，但用的是默认映射。

**根因**：ES 默认允许写入时自动建索引。自动建出来的索引**没有 IK 分词、没有向量字段**，此时检索会静默退化成"什么都搜不到"。

**教训**：**搜索引擎的索引结构必须由应用显式管理**。要么关掉自动创建，要么保证任何写入前索引已按正确映射建好。这次是靠人工发现，更好的做法是在写入前先 `ensureIndex()`。

### 28. 别用 `taskkill /IM java.exe`

调试时为了杀掉占用端口的旧实例，我用 `taskkill /F /IM java.exe` 把**所有** Java 进程都杀了——包括同时跑着的 Elasticsearch。结果 ES 被误杀，要重启一遍（冷启动又要等几十秒）。

**教训**：按 PID 精确杀，别按进程名批量杀，尤其是本机同时跑着多个 Java 服务（ES、应用、可能的 Milvus 等）的时候。

### 29. 证据闸阈值形同虚设——因为判据本身选错了 ⭐

**现象**：知识库有内容之后，一个**完全不相关**的问题（"平台的火箭发射服务怎么预约"）
向量相关度能拿到 **0.7655**，远超 `min-retrieval-score` 默认的 0.6，**证据闸直接放行**。
真正把它拦下来的是第二道自评——而自评时灵时不灵。用户侧看到的就是"这机器人一半概率在编"。

**怎么发现的**：这个是**被测出来的**，不是被看出来的。写 `M6KnowledgeTest` 时
那条"知识库里没有的内容必须拒绝作答"的用例稳定失败，才发现闸根本没起作用。
当时的第一反应是"阈值调高一点"，但调高的过程中发现不对劲——调多高都不对劲。

**根因不是阈值，是判据。** 标了 34 条样本（24 条有答案 + 10 条没答案）画曲线才看清：

| 判据 | 有答案（中位） | 无答案（中位/最高） | 零漏放分界 | 该分界误拦率 |
|---|---|---|---|---|
| 向量最高相似度 | 0.829 | 0.757 / 0.829 | 0.830 | 54% |
| 精排分（只喂正文） | 0.379 | 0.158 / 0.183 | 0.184 | 29% |
| 精排分（喂标题+正文） | **0.837** | 0.130 / 0.190 | **0.191** | **13%** |

**向量相似度这两类的区间几乎完全重叠**——阈值取 0.6 时误拦 0%、漏放 100%；
取到 0.83 才不漏放，但那时 54% 能答的问题也被拒了。**根本没有可用的分界点。**

为什么向量相似度这么不中用：它是**双塔**——问题向量和资料向量各自独立算好再比，
两者之间没有交互。所以它对"两个向量恰好在同一片区域"敏感，
对"资料里到底有没有回答这个问题"不敏感。语料一多，所有问题的 top1 都落在同一段高分区间里。

**修法**：换成**交叉编码器精排**——把问题和资料**拼在一起**过模型，输出相关性分。
它对"这条资料能不能回答这个问题"是直接建模的，区分度完全不同（见上表）。
链路改成 `混合召回收窄候选 → 精排给候选打分 → 证据闸按精排分判定`，
阈值取 0.19（零漏放点）。

**效果**：漏放率 **100% → 0%**，误拦率 **0% → 13%**。
这个取舍是刻意的：误拦只是多转一次人工，漏放是直接给用户一个编造的答案。

**顺带发现的一个值钱细节：喂给精排的必须是「标题 + 正文」。**

第一版只把正文喂给精排，误拦率停在 29% 下不来。查下来发现一个反直觉的现象：
问"押金怎么退"，对着**正文**精排只有 **0.18**；而把语料里那条的**标题**
（"租设备要交押金吗，押金怎么退"）当问题去精排，能拿 **0.79**。

原因是知识库的**标题本身就是问句形态**（"押金怎么退""哪些设备不能发布"），
和用户提问方式天然对得上；而正文是陈述式的解答，往往一次都不重复问题里的说法。
只看正文，精排没法确认"这条到底在不在回答这个问题"。加上标题之后误拦率 29% → 13%。

**这个细节的另一层意义是一致性**：生成回答时喂给模型的就是"【标题】+ 正文"，
证据闸判的应当和模型看到的是同一份材料，否则会出现"闸照着正文判、模型照着标题答"的错位。
（顺带一提，这也是为什么校准脚本要直接调用实现里那个拼接方法——评估必须测实现真正用的东西。）

**教训（很好的面试素材）**：

1. **指标不work的时候，先怀疑判据，别急着调参数。** "把阈值从 0.6 调到 0.8"是错的努力方向——
   它在一个根本没有区分度的维度上找分界点。**先证明这个维度可分，再谈阈值。**
2. **画曲线比拍数字强的地方**，不是"选得更准"，而是**能看出"选不出来"**。
   如果只是拍个 0.75，这个问题会一直藏着。
3. **"双塔召回"的分数不适合当绝对判据。** 它的用途是**排序**（把相关的排前面），
   不是**判定**（这条到底相不相关）。同样的坑在别的 RAG 系统里也很常见。
4. **代价要显式说出来**：13% 的误拦率不理想，但它是有意识的选择，
   不是"调不出来算了"。面试时讲清楚取舍，比讲一个漂亮的假数字好。

---

## 七之二、M9 知识库上传踩的坑

### 30. 命令行工具输出正确结果却返回非 0 退出码 ⭐

**现象**：接入 LiteParse（`lit` CLI）后，**所有 PDF 上传都报"文档解析失败（退出码 1）"**，
但手动在终端里跑同一条命令却完全正常。

**怎么发现的**：写了个最小 Java 程序把 `ProcessBuilder` 跑一遍，把退出码、stdout 字节数、
stderr 一起打出来：

```
cmd=lit         启动异常: IOException（无扩展名的 sh 脚本，Windows 跑不起来）
cmd=lit.cmd     exit=1 stdout=13860B stderr=(空)   ← 明明有完整输出
```

**根因**：`lit` **会在正常输出结果的同时返回非 0 退出码**——它内部会尝试给页面做
Tesseract OCR，而语言包要从 GitHub 下载（本机到不了），那个失败把退出码带成了 1。
这个失败跟我们无关（我们本来就传了 `--no-ocr`），但它污染了退出码。

**修法**：判据从"退出码是不是 0"改成"**有没有拿到输出**"——
有输出就继续用，只在输出为空时才当失败。顺带记下 exit code 供排查。

**教训（很好的面试素材）**：
1. **外部进程的退出码不是可靠的成功信号**，尤其是不受你控制的开源 CLI。
   要按"产物是否符合预期"判断，而不是按它自报的状态。
2. **这个 bug 的现象和根因隔得很远**：表面是"解析失败"，实际是"OCR 下载语言包失败"。
   把退出码、stdout、stderr 三个都打出来，才一眼看穿。

### 31. 声明了超时配置却没接到客户端上

**现象**：给扫描件做多模态 OCR，21 页里 **14 页返回空**，另外 7 页正常。
日志里是 `ReadTimeoutException`。

**根因**：我在类里写了 `@Value("${rag.parse.ocr.timeout-ms:60000}") private long timeoutMs;`，
**但压根没把它设置到 RestClient 上**——用的是框架默认超时。
而 200 DPI 的 A4 整页转 base64 有好几 MB，上传加推理根本来不及。

**教训**：**配置项声明了不等于生效**（和踩坑记录第 15 条"`ai_agent.priority` 配了也没用"
是同一类问题）。更阴的是它不报错、不警告，只是默默用了另一个值。
修法是在构建客户端时显式设 connect/read 超时。

**现象为什么会误导人**：表现是"OCR 莫名其妙没识别出内容"，
第一反应会怀疑"是不是模型不行/图片质量差"，而真实原因是超时。

### 32. 跨页表格合并判据被页眉页脚废掉了 ⭐

**现象**：实现了跨页表格合并，但在一份 31 页的国标 PDF 上**一次都没触发**——
明明有两页的表格标题都写着"表4 检验项目（**续**）"，是铁证如山的续表。

**根因**：判据是"表格必须紧贴页面首/尾"（上一页以表格结尾 + 下一页以表格开头）。
但**国标、论文这类文档每页都有页眉和页脚页码**——表格永远不可能是页面的第一行或最后一行。

**修法**：允许页面首/尾有**少量**非空行（`EDGE_TOLERANCE_LINES = 2`），
把页眉页脚容忍掉。抽成纯函数 + 合成用例单测（构造带页眉页脚的续页），
这类版面问题在真实文档上很难稳定复现，必须能单测。

**另一个更隐蔽的 bug 出现在同一段代码里**：判断"上一页是否以表格结尾"时，
我传进去的是**当前页的行数组**和**上一页的表格块**——行号根本对不上。
两处都修了才对。

**教训**：版面启发式的判据要**按真实文档的形态去设计**，
不能假设"表格就在页首/页尾"。而且这类逻辑务必抽成纯函数单测——
靠真实 PDF 验证一次要跑 6 分钟还花钱。

### 33. 公式被版面算法误判成表格

**现象**：国标 PDF 里的**公式**被识别成了 markdown 表格：

```
| Ein回转 =∑ i=1∫ n | t2 Uin回转 Iin回转dt t1 | ………………(1) |
|---|---|---|
| Eout回转 =∑ i=1∫ n | ... | ………………(2) |
```

它有表头行、有合法的 `|---|---|` 分隔行、列数也一致——**格式上完全合法**。
照单全收的话，知识库里一半的"表格"会是公式。

**修法**：加三条校验（都在 `MarkdownTableExtractor`）：数据行数 ≥ 2、
单元格不能过长、表头不能像公式（含 `=`/`∑`/`∫` 或以编号结尾）。
其中"数据行数 ≥ 2"最有效——那类假表格无一例外只有 1 行数据。

**教训**：解析器输出的"结构化结果"**必须校验**，不能因为它声称是表格就信。
校验规则要从**实际的坏样本**里总结，不是凭空想。

### 34. Windows 下 mysql 客户端会把 UTF-8 的 SQL 脚本读成 GBK

**现象**：执行建表脚本后，表结构正常，但**所有中文注释都变成了乱码**
（`知识库表格` 变成 `鐭ヨ瘑搴撹〃鏍硷`——典型的 UTF-8 字节被当 GBK 解读后再存成 UTF-8）。

**根因**：Windows 控制台的 `mysql` 客户端默认字符集不是 UTF-8，读脚本文件时按 GBK 解。

**修法**：导入时显式指定：

```bash
mysql --default-character-set=utf8mb4 -uroot -p jxj < src/main/resources/db/migration/V2__knowledge_table.sql
```

**教训**：这个坑**不影响功能**（表名、列名是 ASCII 所以没事），只脏了注释，
所以很容易被忽略、一直留在库里。凡是往数据库灌中文，先确认客户端字符集。

### 35. 同一个 CLI 的不同子命令，对同一种输入行为不一致 ⭐

**现象**：装好 LibreOffice 后，`.docx` 上传能成功了（200），但结果里挂着一条莫名其妙的警告：

```
"warnings": ["扫描页 OCR 失败，这些页将没有文字：null"]
```

一份普通 Word 文档，既不是扫描件，也没有图片，怎么会被判成"需要 OCR"？

**怎么发现的**：把 LiteParse 的两个子命令分别对同一个 docx 跑一遍，结果对不上：

```
lit parse     test.docx  → 正常输出 markdown          ✓
lit is-complex test.docx → Error: LibreOffice is not installed   ✗
```

**同一个文件、同一个 CLI、LibreOffice 明明装了**——`parse` 能用，`is-complex` 不能用。
LiteParse 的这两个子命令在"要不要走 Office 转换"这件事上行为不一致。

**后果**：我的管线是 `is-complex 探测 → 判断哪些页要 OCR → parse`。探测这一步对它来说
虽然返回了结果，但页面的文本覆盖率指标不可信，把正常页判成了"需要 OCR"；
接着去截图（`screenshot`，同样对 Office 支持不好）必然失败，于是冒出那条警告。
**用户看到的是"我传了个正常 Word，它说扫描页 OCR 失败"，完全摸不着头脑。**

**修法**：**Office 文档根本不该进这个分支**。
Word/Excel/PPT 都是从文字格式生成的，**结构上一定带文本层，永远不需要 OCR**；
只有 PDF 才可能是扫描件。所以加一个 `MAY_NEED_OCR = {"pdf"}`，
只有 PDF 才做"要不要 OCR"的探测。顺带把探测本身也改成**非致命**——
探测失败就退化成"不做 OCR"，而不是让整篇解析失败。

**教训（很值得讲）**：
1. **不要假设一个工具的子命令能力是一致的。** 名字相近（`parse` / `is-complex` / `screenshot`）、
   文档里并列写着的命令，对同一类输入的内部支持可能完全不同。
   涉及冷门输入类型（Office、加密 PDF、老格式）时，**每个子命令都要单独验证**。
2. **"这个输入根本不该走到这条分支"往往就是正确的修法**，
   比去兼容工具的不一致行为干净得多。这里 Office 不需要 OCR 是**业务上的事实**，
   顺着它改，问题自然消失。
3. 这条警告的措辞是我自己写的，但**当时的措辞把用户往错误的方向引**——
   "扫描页 OCR 失败"暗示文件本身有问题，而真实原因是内部命令选错了。
   报错信息要描述**系统做了什么**，而不是断言**输入有什么问题**。

### 36. embedding 批量上限：潜伏很久、被新功能才逼出来的 400 ⭐

**现象**：上传一份 15 页的扫描国标，解析成功了（正文 13552 字、4 张表都存进了库），
但文档状态停在 `PARSED`、**切片数为 0**、ES 里一条都没有。
从管理页看就是"传了半天，最后啥也没有"，完全看不出哪一步坏了。

**怎么发现的**：拿真实体量（1.3 万字的正文 + 4 张表）写了个最小复现，异常一下就出来了：

```
400: InternalError.Algo.InvalidParameter: Value error,
     batch size is invalid, it should not be larger than 10.: input.contents
```

**根因**：**DashScope 的 embedding 接口单次最多接受 10 条文本，而代码里 `EMBED_BATCH = 16`。**

**为什么潜伏了这么久**：以前入库的都是零散短文本——内置规则每条 1~2 个切片、平台文章
正文大多是空的（13 篇文章里 9 篇正文只有图）。**没有任何一份文档的切片数能凑满一批**，
所以这个错配从 M6 一直活到 M9。上传功能一上线，一个几千字的文档就有几十个切片，
第一批 16 条直接把接口打爆。

**修法**：`EMBED_BATCH` 改成 10，并写上"这是硬上限，不能再调大"。
顺带把失败状态写进文档记录（`status=FAILED` + `errorMsg`），
否则失败后停在 PARSED、切片为 0，根本无从查起。

**防回归**：新增 `KnowledgeIngestBatchTest`，刻意造一份"切片数远超一批"的文档
（1.3 万字正文 + 4 张表 → 44 个切片），断言最终必须是 `INDEXED`。
批次上限再被改大，这条用例就会红。

**教训（很值得讲）**：
1. **"一直没出问题"不等于"没问题"。** 这个 bug 的正确性完全依赖于
   "历史上没出现过超过 10 个切片的数据"——一个从未被触发的代码路径。
   **新功能会打开新的数据规模**，老代码的隐含假设就会崩。
2. **失败要留下可诊断的痕迹。** 当时库里留下的是一个"半成品"：
   正文存了、表格存了、切片 0 条、状态 PARSED——**这不是任何一个正常的中间状态**。
   如果一开始就把异常写进 `error_msg`，根本不用写诊断脚本。
3. 遇到"上传没反应/没结果"，第一反应应该是**去看数据落在哪一步**，
   而不是猜前端或超时。

### 37. 同步串行调外部服务，把"慢"变成了"看起来像卡死"

**现象**：上传扫描件后，界面一直停在"解析中…"，用户等了十几分钟，
直接来问"你们这个到底能不能上传"。日志里从始至终只有一条
"lit 拿到了输出"，之后什么都没有。

**根因**：两个问题叠加。

**一是串行。** 扫描件每一页都要单独调一次多模态 OCR，而我是 `for` 循环一页一页等。
一份 15 页的扫描件 = 15 次串行的 HTTP 往返，每次几十秒，累计十几分钟。
**但这些页之间完全独立**（各自的图片、各自的请求、顺序只影响结果摆放），天然可以并发。

**二是完全不可观测。** 逐页没有日志、前端只有一个静态的"解析中"，
从任何角度看都分不清"在推进"和"卡死了"。

**修法**：
1. **并发 OCR**（默认 4）：15 页从十几分钟压到 **83 秒**
2. **逐页打日志**：`OCR 完成：第 n/总数 页，xx 字`
3. **前端显示已用时间**，并把预期说清楚（"扫描件每页约 10~20 秒"），
   而不是含糊地写"可能要几十秒"

**教训（很值得讲）**：
1. **同步调用外部服务时，"慢"和"挂"在用户眼里是一回事。** 一旦单次请求可能上分钟级，
   要么给进度反馈，要么改成异步任务 + 轮询——不能只留一个转圈。
2. **独立的任务就该并发。** 这个 `for` 循环没有任何顺序依赖，
   纯粹是写的时候顺手了。并发度 4 就拿到了数量级的改善，成本几乎为零。
3. **可观测性不是"上线再说"的事。** 这次如果没有 `jstack`，
   我只能看着"一直在转"束手无策；而真正该有的是**逐页日志**，
   让"卡在哪一步"一眼可见。

### 38. 找回窗口太窄：对的资料在向量里排第 2，却进不了最终结果 ⭐⭐

**现象**：用户问"挖掘机生命周期可能出现什么危险因素"。知识库里明明有一份
《矿用机械正铲式挖掘机 安全要求》，里面第 10-11 页就是「**表 1 危险一览表**」（32 行）。
但模型拿到资料后回的是："危险因素**列在表1中，但表1的具体内容没有在提供的资料里列出**。"
——**模型自己指出了答案在哪，却看不到它。**

**排查过程（值得复述，因为是层层排除）**：
1. 先怀疑自评误杀（项目里有前科）→ 关掉自评重跑，**还是答不上来**，排除
2. 打印检索命中 → 5 条全是正文段落，**没有一条是表格**（`tableId` 全为 null）
3. 确认表格确实入库了（MySQL 有、ES 有、带 tableId）→ 排除入库问题
4. 直接查 ES 的 **KNN**：**表格摘要排第 2 名**（0.8351，第 1 名 0.8411）
5. 查 ES 的 **BM25**：表格摘要在前 10 名里**根本没出现**
6. 看融合逻辑 → **真相在这里**

**根因**：检索用 RRF 融合两路，而 RRF 的公式是 `1/(k+名次)` **累加**：

```
表格摘要     ：只在向量那路排第 2   → 1/(60+2) = 0.0161
某个正文段落 ：BM25 第 1 + 向量第 5  → 1/(60+1) + 1/(60+5) = 0.0318
```

**只走单路的资料，一定输给"两路都进前几名"的资料**——哪怕它在单路里排第 2。
表格摘要恰好就是这样：语义上它最相关（向量第 2），但关键词路线几乎匹配不上
（"挖掘机生命周期可能出现什么危险因素"这串词，和表格里"序号|危险"的写法没有词面交集）。
最终它被挤出 top-5，**精排从头到尾没见过它**。

**修法**：**召回放宽、精排收紧。**
之前是"RRF 取前 5 → 精排这 5 条"，等于让 RRF 替精排做了取舍；
改成"**召回 20 条 → 精排排序 → 取前 5 条喂模型**"，把取舍权交给真正会判相关性的那一步。

**效果**：同一个问题现在能完整列出 9 类机械危险（挤压、制动装置不稳定、传动装置不稳定、
物料抛射、高压流体喷射、联结松动、电缆卷筒不同步、滑倒跌落、玻璃破碎），
**其中好几条在摘要的前 5 行之外**——证明"命中摘要 → 取回完整表格"这条链路也一起跑通了。

**教训（非常有价值）**：
1. **RRF/加权融合这类"多路合并"天生偏爱"两路都靠前"的结果**，会系统性低估
   "只在某一路极端靠前"的结果。知道这个偏置，才能想到去查单路排名。
2. **融合排序 ≠ 相关性排序。** 融合是为了"别漏"，精排是为了"挑准"，
   两件事不能合并成一步——拿融合的前 K 直接当最终结果，就是让排序算法替相关性判断做决定。
3. **排障要一层层排除，别一步跳到结论。** 这次先排除了自评（有前科、最容易怀疑），
   再排除入库，最后靠"分别看单路排名"才定位到融合环节。
4. 模型说"答案在表1里但表1没提供"是**极其有价值的信号**——它不是答不出来，
   而是在告诉你**检索给它的资料不完整**。这种回答不该被当成"模型不行"。

### 39. ES 的 delete_by_query 挂死，以及"入库后不 refresh"的隐患

**现象一**：上传国标时，解析成功（正文和表格都存进库了），但状态停在 `PARSED`、
切片为 0。异常栈指向 `ElasticsearchClient.deleteByQuery`，**卡满 30 秒超时**
（ES 客户端的默认 socket 超时）。

**排查**：手动打 ES 各接口做对照——

| 操作 | 结果 |
|---|---|
| `GET _count` | 200，0.005 秒 |
| `POST _refresh` | 200，0.03 秒 |
| `POST _search` | 200，0.05 秒 |
| **`POST _delete_by_query`** | **无任何响应，120 秒超时** |
| **`POST _delete_by_query`（换成本项目从不碰的另一个索引）** | **同样超时** |

**换索引一样挂**，所以跟我们的索引、数据、mapping 都无关——**是 ES 实例本身
这个接口挂了**。ES 日志里最后一条是加字段时的 `update_mapping`，之后再没写过日志。
**处置：按 PID 重启 ES**，`delete_by_query` 立刻恢复（0.09 秒）。

> 重启 ES 只能按 PID 杀（`taskkill /F /IM java.exe` 会把应用一起杀掉），
> 这是项目里踩过的老坑（第 28 条）。

**现象二（更隐蔽）**：上传完立刻提问，检索**一条都搜不到**新入库的内容；
手工执行一次 `_refresh` 就全出来了。

**根因**：ES 是**近实时**的，写入后默认要等约 1 秒才可搜。
入库流程从头到尾没有主动刷新，所以"传完马上问"会读到旧视图。
排查时一度以为表格没入库，绕了很大一圈。

**修法**：在 `indexDocument` 末尾显式 `es.indices().refresh(...)`。
入库是低频操作，多花几十毫秒换"传完即可搜"完全值得。

**教训**：
1. **"某个接口挂了"和"服务挂了"是两回事。** `_count`/`_search` 全正常，
   只有 `_delete_by_query` 挂——**要用对照实验把范围缩到具体接口**，
   而不是笼统地说"ES 有问题"。
2. **写检索系统要自己管刷新时机。** 近实时意味着"写完立刻读"是一个需要显式处理的场景，
   尤其当业务流程是"上传完马上提问"时。
3. 两条现象叠在一起，会让人以为是同一个 bug——实际一个是 ES 实例故障、
   一个是我们的代码没刷新。**分开验证才知道要修两个地方。**

### 40. 运行中的应用撞上 `mvn` 编译 → `ClassNotFoundException`

**现象**：上传文档时报

```
ERROR ... Servlet.service() threw exception
  java.lang.NoClassDefFoundError: com/jixiejia/agent/rag/parse/MarkdownTableExtractor
Caused by: java.lang.ClassNotFoundException: ... MarkdownTableExtractor
```

**第一反应是"代码里少了这个类"**——但去磁盘一看，类文件好好的，源码也在。

**怎么定位的**：对时间戳。

| | 时间 |
|---|---|
| 报错 | **15:24:15** |
| `MarkdownTableExtractor.class` 的 mtime | **15:24** |

**根因**：**`mvn` 的 compile 阶段会清空并重建 `target/classes`**。
就在那个窗口里，运行中的 JVM 去加载一个还没重新写好的类 → 找不到。
换句话说：**不是类不存在，是它一瞬间不存在。**

触发场景很日常——应用跑着，我在另一个终端跑了 `mvn test`（或 `mvn compile`）。

**修法**：**别在应用运行时跑 Maven 构建**；真跑了就重启应用。
（JVM 会缓存"没找到"这个结果，所以不重启的话它可能一直报。）

**教训（面试点）**：
1. **`ClassNotFoundException` 不等于"类没写"**——先看磁盘上有没有，
   再看**报错时间和文件修改时间**。这次两个时间戳精确到同一分钟，一眼就定位了。
2. **构建产物目录是运行时和构建工具共享的可变状态**，两者同时用就会撞车。
   生产环境不会有这个问题（部署的是打好的 jar），**只在本地开发时出现**，
   所以更容易误判成代码缺陷。
3. 排查这类问题，**"是不是我刚好在编译"** 应该是第一批怀疑对象之一。

---

## 七之三、路由与会话状态类

> 这一类和第五节的"模型行为类"、第七之二的"工程实践类"是同一个主题：
> **静默失效**。它们的共同点是——不报错、不告警，功能"看起来能用"，
> 只是悄悄做错了事。区别在于前面那些栽在模型和外部工具上，这两个栽在我们自己的判断逻辑上。

### 41. 会话粘性把"完整的新问题"当成追问吃掉 ⭐

**现象**（用户实测的三轮对话）：

| 轮次 | 用户输入 | 路由结果 |
|---|---|---|
| 1 | "有没有二手的挖掘机" | EQUIPMENT_QUERY → EquipmentAgent，正常 |
| 2 | "挖掘机操纵杆和其他零件的距离应该控制在多少" | 仍 EQUIPMENT_QUERY（`stage=sticky`） |
| 3 | "挖掘机生命周期可能出现什么危险因素" | 仍 EQUIPMENT_QUERY（`stage=sticky`） |

第 2、3 轮问的是**安全规范**——知识库里就躺着一份国标，答得上来——
却被一直粘在设备 Agent 上，两轮都回"我帮不上忙"。
**用户看到的是"这机器人不会答"，完全看不出是路由走错了。**

**怎么发现的**：**真实使用中撞出来的，不是测试跑出来的。**
审计日志里记了 `stage=sticky`，一眼看出第 2、3 轮压根没走分类器；
如果日志只记"最终归到哪个 Agent"，这个 bug 会一直查不出来。
（与第 11 条同源：**先有可观测性，才查得出静默失效**——修的时候顺手把"判定依据"记进了审计。）

**根因**：粘性分支的判据写成了

```java
if (byKeyword.isPresent()) { ... }
if (sticky.isPresent()) { ... }   // ← 关键词没命中 + 有粘性 → 无条件沿用，不跑分类器
```

设计假设是"**关键词没命中 = 半截追问**"（"那这个呢""多少钱"）。
但这两件事并不等价——而且**这个坑是我们自己另一条设计决策挖出来的**：
领域名词"挖掘机"刻意没进高权重词表（它会同时出现在出租/求租/需求里，见第 20 条），
所以"挖掘机操纵杆……"这类**完整的新问题必然关键词落空**，然后被粘性无条件吃掉。

**修法**：判据从"关键词没命中"改成"**关键词没命中 且 看起来是追问**"：

```java
boolean looksLikeFollowUp = referenceResolver.needsResolution(message)  // 含指代词，是真回指
        || message.length() <= followUpMaxChars;                        // 或足够短，半截追问的形态
```

都不满足就往下走第 ④⑤ 步老老实实重新分类。复用现成的 `ReferenceResolver.needsResolution()`，
新增配置 `routing.sticky.follow-up-max-chars`（默认 12）。

> ⚠️ **上面这条"字数 ≤ 12 也算追问"的判据后来被删掉了**——它只是把"猜"换了个地方。
> 续集见**第 48 条**：中文一句话信息密度高，"挖掘机驾驶室有什么要求吗"正好 12 个字、
> 是一句完整的问题，却照样被判成追问。**现在只认"含指代词"这一条语义判据。**

**成本影响有限**：多走的这一步第一层是本地小模型（免费），真正有云端成本的只有 4% 走到大模型。

**验证**：重放用户那三轮——第 2 轮答出"操纵力不超过 50N 时距离不小于 25 毫米"
（正是国标里的技术参数），第 3 轮列出 9 大类危险。
新增 2 个用例：**长的新问题不走粘性、短的半截追问仍走粘性**——修洞不能把正路堵了。

**教训（很值得讲）**：

1. **两个不等价的条件被当成等价用了**——"关键词没命中"和"是追问"。
   这种错误的等价替换在做优化时特别常见：想的是"这里能省掉一步"，
   而不是"这一步到底在判什么"。
2. **"省钱的优化"出问题时，会伪装成"能力不足"。** 粘性是为了省一次分类调用，
   它坏掉的表现是"答不上来"，不是任何形式的报错——于是所有人（包括用户和我们自己）
   都会去怀疑模型、怀疑检索，没人会去怀疑路由。
3. **一条设计决策会成为另一个 bug 的土壤。** "领域名词不进高权重词表"（第 20 条）
   本身是对的，但它让"不含关键词的完整新问题"变成一种常见输入；
   而粘性那条判据当初是在"关键词没命中很少见"的假设下写的。
4. 修这类**兜底/快路径逻辑**，一定要同时补一条"正路没被堵"的用例。

### 42. 换个构造参数重建对象，把字段丢成了 null

**现象**：答案溯源做完之后，**表格类问题**的回答在溯源列表里**没有出处**——
编号后面是空的（`[2] · 第 ? 页`），而其它类型的题都正常。

**根因**：`KnowledgeAnswerService.expandTables()` 在"用完整表格替换摘要"那一步
需要重建命中对象，换构造方式时**少传了两个参数**，`sectionPath` / `pageNo`
被静默补成了 `null`；而溯源列表正是照着这两个字段渲染的。

**为什么难发现**：它**不报错、也不会让任何断言变红**。溯源是"锦上添花"的功能，
缺了出处不影响答案本身对不对——只会让用户看到"这个来源怎么是空的"。

**修法**：补齐参数。**顺带记一条**：给 record / 长参数列表的对象**加字段**时，
要逐个检查所有重建它的地方——加了字段之后，旧的构造调用会静默补默认值，
**编译器不会提醒**。这和"配置项声明了不等于生效"（第 15、31 条）是同一类问题：
**缺省值让"少写了什么"变得不可见。**

### 这两个坑的共同点（面试可以这么收尾）

都是**"我以为是 A，代码判的是 B"**：
粘性那条把"关键词没命中"当成了"是追问"，
`expandTables` 那条把"重建对象"当成了"只换内容、元数据无所谓"。
**两者都不报错**，都要靠"可观测性"或"可验证的收敛判据"才能发现
（前者是审计日志里的 `stage`，后者是"溯源列表里每一条都该有出处"）。

---

### 48. 粘性的判据又猜了一次——同一个毛病犯了两次 ⭐⭐

**现象**（用户实测并主动贴了 DEBUG 日志）：

```
粘性兜底：挖掘机驾驶室有什么要求吗 -> RentalAgent
粘性兜底：安全要求呢              -> RentalAgent
```

用户的原话是"**为什么每次都是粘性兜底，每次都误判**"。
"挖掘机驾驶室有什么要求吗"是知识类问题，却被粘在了租赁 Agent 上。

**这是第 41 条的续集**——那条修的是"关键词没命中就无条件用粘性"，
修法是补一条"**看起来像追问**"的判据。这次要修的就是那条判据本身。

**根因**：第 41 条补的判据是**两条**——

```java
boolean looksLikeFollowUp = referenceResolver.needsResolution(message)  // 含指代词
        || message.length() <= followUpMaxChars;                        // 或足够短（默认 12）
```

第二条是**字数**。而中文一句话的信息密度高：

| 输入 | 字数 | `<= 12` | 结果 |
|---|---|---|---|
| 挖掘机驾驶室有什么要求吗 | **正好 12** | ✅ | 判成"追问" → 粘住 |
| 安全要求呢 | 5 | ✅ | 同上 |

再加上第 20 条那个设计决策的副作用：**领域名词"挖掘机"刻意不在高权重词表里**
（它同时出现在出租/求租/需求里），所以这类问题**必然关键词落空**，
必然掉进"像不像追问"这一关——而这一关的判据是**猜**的。

**修法**：**把字数判据整个删掉**（连 `routing.sticky.follow-up-max-chars` 配置项一起删），
只留"含指代词"一条：

```java
boolean looksLikeFollowUp = referenceResolver.needsResolution(message);
```

**为什么留这一条就够**：指代词（"这个/那台/它"）**脱离上文根本无法理解**，
所以含指代词几乎必然是追问——这是**语义**判据，不是形态判据。
反过来，不含指代词的短句（"多少钱""还有吗"）字面意思已经完整，
交给第 ④⑤ 步连同上文一起判，比猜准得多。

**代价（照实记）**：这类短句要多跑一次本地小模型。**免费**，但会慢 1~3 秒。
换来的是**路由可预测**。

**教训（这是本轮最值钱的一条）**：

1. **同一个毛病我犯了两次，两次都是"用形态特征去猜语义"。**
   第一次猜"关键词没命中 = 追问"，第二次猜"短 = 追问"。
   **形态判据（长度、有没有某个词）永远只能逼近语义，而反例总在你想不到的地方**——
   这次的反例是中文的信息密度。
2. **一个坑修完，要问一句"我这次补的是判据，还是又一个猜测？"**
   第 41 条的修法在当时看是合理的（"短句多半是追问"确实成立），
   但它把"猜"从一层挪到了另一层，而不是消掉。
3. **修完之后，凡是"猜"的地方都该有测试钉住它的边界**——
   第 41 条只测了"长的完整新问题"和"短的追问"两头，
   **恰好漏掉了"短的完整问题"这个中间地带**，而 bug 就长在那里。

**面试怎么讲**：这条比单讲第 41 条有说服力得多——
**"第一次我以为是判据写错了，改了；第二次发现是我在'猜'，把猜的判据换成了语义判据。"**
能主动说出"我在同一个地方栽了两次、第二次才找到根因"，比只讲一次成功的优化可信。

---

## 七之四、原件落盘与引用角标（本轮：让答案里的 [1] 点得下去）

> 这一轮的目标是"溯源闭环"：上一轮已经能说出"依据来自国标第 10 页"，
> 但**那句话点不下去**——原件根本没存。这五条是把原件存下来、接上去的过程里踩的。

### 43. 扩展名里带路径，能爬出落盘目录 ⭐

**现象**：上传的原件要落盘，文件名是 `sourceId + "." + ext`。而 `ext` 来自**用户上传的原始文件名**——
看起来人畜无害，实际上是一个可以带路径的字符串。

**怎么发现的**：**代码质量审查查出来的，不是跑出来的。** 审查者顺着 `ext` 的来源链往上查：

```
AdminKnowledgeController.upload
  → String ext = DocumentParser.extensionOf(filename);
  → DocumentParser.extensionOf()   // 561-568 行
      return filename.substring(filename.lastIndexOf('.') + 1);
```

**返回的是最后一个点之后的全部内容**。文件名写成 `a.xyz/../../foo` 时，
它交出来的就是 `xyz/../../foo`——里面带分隔符和 `..`。拼进路径后 `Files.write` 会沿路径往上爬。

**根因**：写路径**根本没设防**——`resolve()` 的越界校验只加在了**读**路径上，
写路径直接 `root.resolve(name)`。而对一个"用户上传内容进文件系统的唯一入口"来说，
这是最不该不对称的地方。

更讽刺的是：**项目自己在 `DocumentParser.java:246-250` 写过这条纪律**——
"用户提供的文件名不参与路径拼接，避免路径穿越"。我在新增的类里把它破了。

**修法**：白名单，而不是黑名单。只放行 `[a-z0-9]{1,10}`，认不出来就当"没有扩展名"；
再走一遍 `resolve()` 做第二道。**白名单的好处是它不挑攻击载荷**——
不用去想"要拦哪些坏字符"，反斜杠、`..`、UNC 路径自然都进不来。

**教训（面试点）**：
1. **安全校验要问"写路径设防了吗"，不能只看读路径。** 审查者原话：
   "写路径不设防、只有读路径设防，是不对称的。"
2. **白名单比黑名单可靠**——黑名单要穷举攻击者的想象力，白名单不用。
3. 项目里写过的纪律，在新增的同类代码上要**主动复用**，不能因为"这是新写的"就重新想一遍。

---

### 44. 120 秒超时是个摆设：`readAllBytes` 先阻塞，永远走不到 `waitFor`

**现象**：`convertOfficeToPdf` 调外部命令 `soffice` 转 PDF，声明了 120 秒超时。

**怎么发现的**：同样是代码质量审查推演出来的。看这段的顺序：

```java
String output = new String(process.getInputStream().readAllBytes(), UTF_8);  // ①
if (!process.waitFor(120, TimeUnit.SECONDS)) { ... }                          // ②
```

**`readAllBytes()` 会一直阻塞到 stdout 到达 EOF**。所以进程活着但卡死（不再输出、也不关管道）时，
① 永远不返回，主线程根本执行不到 ②——**超时只约束了"EOF 到进程退出"这段几乎为零的时间**。

**根因**：把"读输出"和"等进程"写成了串行，而这两件事必须并行——
子进程的输出管道写满了它就会阻塞，你不读它就永远不结束；而你要等它结束才肯往下走。

**修法**：读输出放到独立线程，主线程先 `waitFor(timeout)`；超时就 `destroyForcibly()`，
**并且再 `waitFor(10s)` 等它真的死掉**（不补这一下，Windows 上会留僵尸进程占着文件句柄）。

**顺带**：项目里 `LiteParseClient` 是**同一个写法**——而它管的正是最慢的 OCR 路径。
已单独记为待办（这条超时失效比"慢"更糟：是**永久挂住请求线程**）。

**面试点**：**配置项声明了不等于生效**——这条和第 15、31 条是同一类，
只不过前两条是"没接到客户端上"，这条是"接到了但被上游阻塞绕过"。

---

### 45. 同一条判据写在两个地方，必然漂移 ⭐

**现象**：给前端判断"这份原件能不能预览"的逻辑，和预览接口里"该取哪个文件"的逻辑，
**是两个类各写一遍**。而在计划稿阶段就已经漂了：

| 位置 | 对 `previewPath` 的判据 |
|---|---|
| `KnowledgeAnswerService.previewable` | `!= null` |
| `KnowledgeFileController`（草稿） | `isBlank()` |

于是 `previewPath == ""`（空串）时：答案服务告诉前端"能预览"，点开却 404——
**正是两边注释里都写着"必须保持一致"的那个场景**。

**根因**：把**"能不能预览"和"该取哪个文件"当成了两个决策**。它们其实是一个：
"这份文档有没有一个能交给浏览器的文件，是哪个相对路径"。拆成两半，
调用方就必须把"Office 取预览件、其余取原件"这条分支再写一遍，两遍迟早不一致。

**修法**：收口成 `KnowledgeFileStore.previewFileOf(filePath, previewPath)`，
**返回路径，null 表示不可预览**——两边都从它投影。
刻意返回路径而不是布尔值：这样取件端不用再写一遍分支。

**面试点（很值得讲）**：
1. **发现"规则要写两遍"的时候，就是该抽方法的信号**——而不是"先写着，以后再统一"。
   这次是审查者在第二个消费方还没落地时就拦下来的，改起来只花了几分钟；
   等两边都上线再改，就得先 debug 一遍"为什么点不开"。
2. **抽方法时选对返回类型**：返回"决策结果"（路径 / null）比返回"决策的一部分"（布尔）
   更能消掉重复——布尔值只回答了"能不能"，没回答"取哪个"。

---

### 46. 删了文档，磁盘上的原件还在 ⭐

**现象**：删除知识文档后，`data/knowledge-files/` 里对应的原件文件**原封不动躺着**。

**怎么发现的**：**端到端验证时撞出来的。** 跑完一圈接口验证，顺手 `ls` 了一下落盘目录——
发现删掉的那几个文档，文件全在。测试用例也是：跑完一轮测试就多几个文件。

**根因**：`deleteDocument` 清了三处——ES 切片、MySQL 切片、MySQL 表格——**唯独没碰文件**。
而这个类**没有任何别的清理机制**：ES 那边至少有 `reconcileIndex()` 对账兜底，
文件这边连对账都没有。用户删了文档以为东西没了，原文却永远留在磁盘上。

**修法**：`deleteDocument` 里对 `filePath` / `previewPath` 各调一次 `deleteQuietly`
（清理失败只记日志，不该把"文档已删"这个结论推翻）。新增用例锁定行为。

**数字**：修之前，跑完一轮测试落盘目录会多几个文件；修之后跑 42 个测试，**残留 0**。

**教训**：**"删除"这件事的完整性，要按"数据散落在几个地方"来数。**
这个项目里一份知识文档的数据散在 **4 处**（ES / MySQL 切片 / MySQL 表格 / 磁盘文件），
少清一处就是一处泄漏，而且**都不报错**。

---

### 47. 指纹去重只覆盖"重传同一份"，失败路径照样堆孤儿

**现象**：上传的原件用**内容指纹**命名，注释里写着"重复上传连文件都不会堆第二份"。
听起来像是不会有堆积——**其实不成立**。

**怎么发现的**：代码质量审查**逐条推演**了四种失败路径：

| 路径 | 磁盘上留下什么 | 有 DB 行引用吗 |
|---|---|---|
| 解析抛异常 → 400 | `哈希.ext` | **没有** |
| 解析成功但没文字内容 → 400 | `哈希.ext` | **没有** |
| 入库跳过（内容没变）→ 200 | 覆盖同名文件 | 有 |
| 入库抛异常 → 500 | `哈希.ext` | 行在，但 `file_path` 是 null |

**根因**：指纹保证的是"**同一份内容**不会堆第二份"，不是"不会有任何堆积"——
**内容不同的坏文件，各有各的指纹，就是一个一个新文件**。

**修法**：
- 两条 **400 驳回**路径清掉刚落的原件（上传被驳回了，用户手里还有原文件，
  磁盘上留个没人引用的副本没价值）
- **500 路径故意不删**：文档行已经建了（FAILED），留着原件能让人工重试和排查
- 把注释改准——**原来那句"连文件都不会堆第二份"把保证范围说宽了**

**教训（很值得讲）**：
1. **写"某某保证"的注释时，要问一句"它的边界在哪"。** 一句读起来像"万无一失"的注释，
   会让后来的人不再去检查那条路径——比没有注释更危险。
2. **指纹/哈希这类去重手段，去的是"相同"，不是"垃圾"。** 垃圾是**各不相同**的，
   哈希对它无能为力。

---

## 七之五、文档解析（分页与 OCR）

### 49. "兜底"其实不兜底：不可变列表让整篇 OCR 白做 ⭐

**现象**：**传一张单页扫描的 PDF，OCR 一定失败**——而且不报错，只在结果的 warnings 里留一条
"扫描页 OCR 失败，这些页将没有文字：…"，这一页就永远是空的。

**怎么发现的**：用户报的是另一个问题（第 50 条，日志里"第 28/21 页"看着自相矛盾）。
查那条日志的分母时，顺着 `applyOcr` 往下读，看到了这一行：

```java
pageTexts.set(pageNo - 1, text);   // ← 把 OCR 结果写回页列表
```

而 `pageTexts` 来自 `splitPages`，它的两条**兜底分支返回的是 `List.of(markdown)`——
不可变列表**：

```java
if (expectedPages > 1 && separators.size() != expectedPages - 1) {
    warnings.add("分页符数量（%d）与页数（%d）对不上，本次不做分页处理（跨页表格可能无法合并）");
    return List.of(markdown);      // ← 不可变
}
if (separators.isEmpty()) {
    return List.of(markdown);      // ← 不可变
}
```

**根因**：`java.util.List.of(...)` 返回的是 `ImmutableCollections$ListN`，
对它调 `set()` 直接抛 `UnsupportedOperationException`（已用最小程序验证）。
于是：

```
走到兜底 → 页列表不可变 → applyOcr 里 set() 抛异常
        → 被 applyOcr 外层的 catch 兜住 → 整篇 OCR 结果全丢，只加一条 warning
```

**最典型的触发场景是单页扫描件**：它本来就没有分页符，`separators` 为空，
**必然走第二条兜底**。也就是说"单页扫描 PDF" = "OCR 必失败"。

**为什么藏了这么久**：两层掩护。
1. 那条 warning 的措辞是"本次**不做分页处理**（跨页表格可能无法合并）"——
   读起来只是丢了个锦上添花的功能，**实际上是 OCR 全废**。
   这是项目里第 35 条那个教训的反面：**报错信息要描述系统做了什么，而不是轻描淡写地断言影响范围**。
2. `splitPages` 是 private、跑真实文档才走到，而**能稳定触发它的样本（单页扫描件）
   恰好是测试里没有的**——现有的解析测试用的都是多页国标。

**修法**：抽一个 `singlePage(markdown)` 返回可变列表，两条兜底都走它，
并把"为什么必须可变"写进注释。**顺带把 `splitPages` 改成包级可见**，好直接单测。

**验证方式**（这次特意做了"证明测试抓得到"这一步）：
测试断言的是**行为**——`pages.set(0, ...)` 不能抛异常。为了确认它不是空转，
另外用最小程序证明了 `List.of().set()` 确实抛 `UnsupportedOperationException`。

**教训（很值得讲）**：

1. **兜底路径要单独问一句"它真的兜住了吗"。** 兜底代码通常写得最随意、
   跑得最少，而它一旦不兜底，**损失的是主干功能**（这里是 OCR），
   不是"少了个附加能力"。
2. **`List.of()` / `Map.of()` 这类不可变工厂，在"需要修改"的路径上是陷阱。**
   它们读起来比 `new ArrayList<>()` 干净，但**返回类型是接口，可变性不在签名里**——
   调用方看不出来。
3. **warning 的措辞会决定它被不被当回事。** 写"不做分页处理"和写
   "OCR 结果全部丢失"，是同一件事的两种说法，而后者才会有人去查。

---

### 50. 进度日志把两个不同的量拼成了一个分数

**现象**（用户报的原始问题）：

> 为什么 PDF 一共 21 页，进度表能切 30 多

日志长这样：

```
OCR 完成：第 21/21 页（页码 21），791 字
OCR 完成：第 28/21 页（页码 28），165 字
OCR 完成：第 29/21 页（页码 29），3 字
```

**怎么发现的**：用户直接把 DEBUG 日志贴过来了。

**根因**：日志的分子是**文档里的绝对页码**，分母是**需要 OCR 的页数**——
两个不同的量放在一个分数里：

```java
log.info("OCR 完成：第 {}/{} 页（页码 {}），{} 字",
        pageNo,              // ← 绝对页码
        ocrPages.size(),     // ← 需要 OCR 的页数
        pageNo,              // ← 又打了一遍
        ...);
```

那份文档**实际是 31 页**（`lit is-complex` 与独立的 PyPDF2 数出来一致，都是 31），
其中 **21 页**文字覆盖不足需要 OCR。所以分母 21 是"要 OCR 的页数"，不是"文档总页数"。
用户看到的 28/29 是页码，不是进度——而且并发度是 4，**日志本来就是乱序的**。

**修法**：分子改成**完成计数**（`AtomicInteger`），页码另标：

```
OCR 完成：第 3/21 页（原文第 28 页），165 字
```

这样两个量各归各位：进度看"第几个完成"，身份看"原文第几页"。
顺带把重复打印的 `pageNo` 去掉，并给"开始 OCR"那条补上"文档共 N 页"作为对照。

**教训**：

1. **日志里的分数，分子分母必须同量纲。** 混着放不会报错，只会让人读出**错误的结论**
   ——用户就是因此以为那份 PDF 只有 21 页，反过来质疑解析器。
2. **并发流程里，"第几个"和"第几个完成"是两回事。** 只打页码的话，
   日志看起来像跳号的进度条（1、5、2、7…），越看越像 bug。
3. 一条日志的成本极低，但**它写错了会主动误导排查方向**——
   用户原本是来问"文档解析对不对"的，被这条日志带偏成了"页数怎么算错了"。

---

## 七之六、意图识别（词表的天花板）

### 51. 发布意图认不出来——"用词表判定案"的第三次翻车 ⭐⭐

**现象**（用户实测）：

> 我想出租一台洋马60

系统的回答是"平台上暂时没有洋马60的出租信息，你可以试试放宽条件…"——
**把发布判成了查询**。（有意思的是它自己都察觉到了，最后补了一句
"如果你是想发布自己的洋马60出租信息，可以到平台上发布"。）

**怎么发现的**：用户直接把对话贴过来问"为什么这个识别不出来出租，我说的这么清楚"。

**这是同一个结构问题的第三次发作**：
- 第 19 条："帮我发布一台出租" 被"出租"抢走
- 第 20 条：领域名词（挖掘机）当高权重词，让"附近有挖掘机出租吗"被误判成设备查询
- 这次："我想出租一台洋马60" 被"出租"抢走

三次都是**用一份词表去逼近一个语义**。前两次的修法都是"把词表调得更准一点"，
所以第三次照旧会发生。

**根因**：`出租` 既是"我出租我的设备"（发布）也是"有没有出租的"（查询），
**词表分不清**。而发布意图的表达是**开放集合**：

```
我想出租 / 我要出租 / 我打算把设备放平台出租 / 我有台挖机想挂出去
/ 帮我发个求租 / 我这边有台小松200想放出去赚点租金 …
```

枚举整句永远会漏，而**每漏一句，这句话就会被"出租""求租"这些查询词抢走——
而且是 0.90 高置信地抢走**（错得毫不含糊）。

**这一轮没有直接改代码，先补了量尺**——因为原来的 26 条评估集里"发布"只有 1 条，
而且恰好是"帮我发布一台出租"（词表专门为它写过），**改坏了也看不出来**。
扩到 63 条、加了"发布 vs 查询""跨域 vs 不该跨域"两组对照后，量出来的基线是：

```
63 条，55 正确，87.3%
发布意图 9 条错 4 条，其中 3 条是关键词层 0.90 高置信直出
```

**修法**（三件事）：

1. **`发布` 从发布意图的定案词里拿掉**。它太泛："发布设备要审核多久"里的"发布"
   是名词、说的是审核流程，却曾被 0.90 判成"发布出租"——这是**反向误判**。
2. **新增判据 `looksLikePublishIntent`**：意愿词（想/要/打算/准备/帮我…）×
   发布动作词（出租/租出去/挂出去/放出去/发布/发个求租）**同现**即命中，
   **不要求相邻**（"我打算把设备放平台出租"中间隔了六个字）。
3. **命中后只"让路"、不"定案"**，并且**把"这是发布意愿"当提示喂给模型**。

**第 3 条是量出来的**，中间走了一小段弯路，值得完整讲：

| 做法 | 准确率 | 为什么 |
|---|---|---|
| 让关键词直接判 | 87.3%（基线） | 0.90 高置信判错 |
| 命中后**只让路**（不给任何提示） | — | "我想出租一台洋马60"**照样错**（small 0.92）：7B 小模型缺"主体是机主"的证据 |
| 命中后**当提示喂给模型** | 93.7% | 模型拿到"这像发布意愿"的线索后判对了 |

**关键区别：提示是建议，模型能推翻；定案是结论，错了就是错的。**
同一条判据，当提示用是安全的，当判据用是危险的。

**量出来的完整归因**（每一版都跑同一套 63 条）：

```
基线                                        87.3%（55/63）
+ 去掉"发布"定案词 + 让路规则                 92.1%（58）
+ 描述改成又长又强调"主体"                    90.5%（57）← 退步，已回退
+ 把发布意愿当提示喂模型                      92.1%（58）
+ 回退那些冗长描述（只留修好了 QIUZU 的那条）    93.7%（59）
```

> 顺带确认了这些差异**不是噪声**：小模型（Ollama）设了 `temperature: 0.0`，
> 是确定性的；只有主模型没设温度、有随机性，但它只承接 2% 的请求。

**结果**：发布意图 **9 错 4 → 9 错 1**；用户报的那句已修好。
层分布 48% / 51% / 2%（关键词层占比降了，多出来的走**本地**小模型，云端仍是 2%）。

**还走了一小段弯路，也值得记**：动作词表最初收了"求租"，于是"我想求租"命中了让路规则，
两个路由测试直接红了。想清楚才发现——**"求租"是需求类型，不是发布动作**：
"我想求租"是承租方想找机器，真正表示发布的是"**发**个求租"里那个"发"。
改成"发个求租/发条求租"后测试全绿（**没改测试**，是代码回到正确）。

**教训（这是本轮最值钱的）**：

1. **"用词表判定案"有天花板，而且到顶了看不出来。** 前两次都以为"这次把词调准了"，
   第三次照旧。真正的信号是：**每次修都是"再加几个词"，而不是"换个判据"**——
   当修法开始重复自己，说明问题的层级没找对。
2. **同一条判据，"当提示"和"当定案"的风险完全不同。** 提示错了模型能推翻，
   定案错了就是错的。所以**不确定的时候，把它降级成提示**——这是比"删掉"更细的一档处理。
3. **补量尺要先于改代码。** 旧评估集只有 1 条发布样本、且是词表专门写过的，
   它给错误行为背书（测出来 96.2%）。换成 63 条后基线掉到 87.3%——
   **那不是系统变差了，是原来的尺子量不出来**。
4. **想当然的动作词会立刻被测试抓住。** "求租"那条要是没测，会一直错下去，
   而且错得很隐蔽（只是让路变得更容易，不报错）。

**面试怎么讲**：这条能串起前三轮（第 19、20 条 + 这条），说成一条线：
**"同一个错我犯了三次，第三次才意识到问题不在词表，在'用词表判定案'这件事本身。"**
比单讲一次成功的优化可信得多。而且能顺带讲"提示 vs 定案"这个更细的取舍。

---

### 52. 跨域判定：过的那几条，是靠一个副作用过的 ⭐⭐

**现象**：评估集里"有没有20吨挖机、附近能租吗、流程是啥"期望 CROSS_DOMAIN，
实际判成了 KNOWLEDGE_QUERY——**跨域没被识别出来**。

**怎么查的**：先确认"执行层是不是有问题"——不是。`CompositeGraph` 的并行扇出
（各域 Agent 同时跑 + 汇总）一直是好的，而且第 15 条那个"被单 Agent 吃掉"的坑早就修了。

再确认"模型是不是判不出来"——也不是。评估集里 4 条跨域样本**过了 3 条**。

于是去看**它们是怎么过的**，这一看就找到了根因：

| 样本 | 关键词层发生了什么 | 结果 |
|---|---|---|
| 有二手的挖掘机吗，另外有没有挖掘机出租 | 设备("二手" 0.9) 和出租("出租" 0.9) **同分 → 不下结论** | 下沉给模型 → 判对 |
| 想买台挖机，也想知道有没有出租的 | 同上（"想买" vs "出租" 同分） | 模型判对 |
| 有没有车源，另外有没有活干 | 同上（"车源" vs "有活干" 同分） | 模型判对 |
| **有没有20吨挖机、附近能租吗、流程是啥** | **只有 "流程" 一个强词**（0.9），"租" 是弱词（0.7） | **关键词层直接定案** ✗ |

**根因**：**过的那 3 条靠的是"两个高权重词同分"这个副作用**——
也就是第 16 条说的那个"保护"（所有高权重词都是 0.9 分，两个域同时命中就必然同分，
判不出来只能下沉给模型）。它们**不是靠跨域判定本身过的**。

而漏的那条恰恰是**只有一个域命中了强词**：关键词层 `top > runnerUp` 成立，直接定案，
**模型连看一眼的机会都没有**。

**所以这个问题的性质是**：不是"框架不行"，也不是"模型不行"，
是**关键词层抢答，把模型挡在了门外**。

> 顺带回答一个很容易问的问题：**LangGraph4j / ReAct / plan 模式能不能解决这个？**
> 不能——它们回答的是"**给定目标之后怎么执行**"（调哪些工具、什么顺序、要不要迭代），
> 而"这句话里有几个目标"是执行**之前**的判断。
> plan 模式是"先想清楚再动手"，但它得先被叫起来；现在的毛病是它压根没被叫起来。

**修法**：给关键词层的定案条件加一条——**除了"没人和我并列"，还要求"没有别的业务域也在说话"**。

**这条规则收紧过两次，两次都是被测试/评估集当场抓住的**：

1. **第一版**（"任何别的意图有命中就不定案"）**太宽**：
   `"求租"` 这个词本身含 `"租"`，而"租"是出租查询的弱词——于是**每一句带"求租"的话
   都被判成"两个域在说话"**，求租意图的快速通道被整个堵死。路由测试直接红。
   **收窄**：命中词全落在我命中词内部的，算"同一处文本"，不算另一个域。
2. **第二版**（仍然太宽）：`"这个平台怎么投诉"` 命中了知识意图的弱词 `"平台怎么"`，
   投诉的快速通道被挡掉。路由测试又红。
   **收窄**：**"别的域"只算业务域**——就是 `CROSS_DOMAIN` 自己声明的那 5 个
   （设备/出租/求租/需求/资讯）；知识、发布、投诉、闲聊**不参与跨域**。

**效果**：

```
准确率        93.7% → 95.2%（59 → 60 / 63）
跨域意图      3/4  → 4/4
关键词层直出   48% → 40%（少判的那些走本地免费小模型，云端仍是 2%）
```

**教训（很值得讲）**：

1. **"通过"和"因为正确而通过"是两回事。** 那 3 条跨域样本一直是"通过"的，
   但它们是靠一个**与跨域无关的副作用**过的。如果只看准确率，
   会以为"跨域这块没问题"——**要去看"它是怎么过的"，不能只看过没过**。
2. **同一个机制，在一个场景里是保护、在另一个场景里就是漏洞。**
   "两个高权重词同分 → 下沉给模型"本意是防"用关键词的枚举顺序决定路由"（第 16 条），
   结果它顺带成了跨域的唯一救命稻草——而这条稻草只在"两个域都命中了强词"时才在。
3. **收紧判据时，第一版几乎一定太宽。** 这次两版都被测试当场抓住，
   而且抓得很快——**这正是"先扩评估集、并给路由层补测试"的回报**。
   两条收窄（同一次提及的排除、只算业务域）都不是想出来的，是跑出来的。

---

## 八、知识库语料说明

库里原本只有 13 篇文章，**其中 9 篇正文只有图片**，剩下 4 篇的正文长度是 10、38、13、8 个字——等于没有内容。用它演示"问规则"是演示不出来的：证据闸永远拦下，看起来像功能坏了。

所以补了一份**内置示例语料** `src/main/resources/knowledge/platform-faq.md`，12 条贴近真实业务的平台规则（服务费率、押金退还、租赁流程、发布审核、违规处理等），启动时自动入库。

> ⚠️ 这是**演示用示例语料，不是平台真实政策**。真实上线要替换为运营确认过的正式文本。

这样系统的开箱表现才正常：能答的答，答不了的老实说不知道并转人工。

---

## 九、面试时怎么组织这些内容

### 如果只让你讲三个

1. **证据闸判据选错**（第 29 条）——体现你会用数据判断"这个方向对不对"，
   而不是调参数调到满意为止；而且知道**双塔相似度适合排序、不适合判定**这个本质
2. **静默降级**（第 11 条）——体现你会做可观测性、知道"能跑"和"跑得好"的区别
3. **自评误杀**（第 25 条）——体现你懂提示词的细节陷阱，且知道"防幻觉"本身也会出错

**第 15 条（跨域被单 Agent 吃掉）**也是好素材：体现你会查"为什么配置没生效"，
而不是只看配置写没写。**第 13 条（大模型会编平台规则）**则是证据闸存在的理由本身。

**第 41 条（粘性吃掉完整新问题）**属于同一类，而且更完整——
它把"静默失效"这条线串起来了：**第 11 条**教你先做可观测性、
**第 41 条**是这条经验的兑现（靠审计日志里的 `stage=sticky` 定位）、
**第 20 条**（领域名词不进高权重词表）是它的土壤。
被问到"你怎么定位线上问题的"时，用这一条讲**排查路径**最顺。

### 高频追问及准备

**Q：怎么保证 AI 不乱答？**
三层：证据闸（生成前，资料不够不让开口）→ 提示词硬约束（规则类问题不许凭常识答）→ 生成后自评（核对是否完全基于资料）。另加数据飞轮收集答不上来的问题，人工补库。

**Q：多 Agent 怎么选？**
显式映射，不是猜：意图 → 能力 → Agent 对照表 + 优先级 + 角色权限。跨域时按"命中域数 ≥2"走综合子图。

**Q：成本怎么控制？**
三层意图识别，40% 关键词直出（零成本）、59% 本地小模型（免费）、只有 2% 走到云端大模型。会话粘性让追问不重复分类。

**Q：失败了怎么办？**
分层降级而不是重试：小模型超时 → 大模型；大模型也不行 → 兜底 Agent；检索失败 → 证据闸拦下并转人工。目前**没有**自动重试——因为模型调用超时后重试可能造成重复计费，且对话场景下用户更在意响应速度。
